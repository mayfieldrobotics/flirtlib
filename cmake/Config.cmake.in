set(flirtlib_INCLUDE_DIRS @CMAKE_INSTALL_PREFIX@/include/flirtlib)

set(ALL_FLIRT_LIBS "feature;geometry;gui;sensors;sensorstream;utils")

foreach(lib ${ALL_FLIRT_LIBS})
    list(APPEND flirtlib_LIBRARIES @CMAKE_INSTALL_PREFIX@/lib/flirtlib/lib${lib}.so)
endforeach()

